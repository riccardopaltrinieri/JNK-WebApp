package dao;


public class UserDAO {

	public UserDAO() {}
	
	public boolean checkCredentials(String usrn, String pwd) {
		//String query = "SELECT  id, role, username FROM user  WHERE username = ? AND password =?";
		
		return true;
	}
}
